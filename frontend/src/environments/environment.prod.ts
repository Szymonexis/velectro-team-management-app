import { Environment } from './environment.type';

export const environment: Environment = {
  isProduction: true,
  // TODO: Change when production server is ready
  baseUrl: 'http://localhost:3030/api',
  appName: 'Velectro team manager',
};
